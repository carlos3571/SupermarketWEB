@page
@model SupermarketWEB.Pages.Provider.DeleteModel
@{
    ViewData["Title"] = "Delete";
}

<h1>Delete</h1>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Category</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Provider.Document)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Provider.FirstName)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Provider.LastName)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Provider.Addres)
        </dd>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Provider.PhoneNumber)
        </dd>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Provider.Email)
        </dd>
    </dl>

    <form method="post">
        <input type="hidden" asp-for="Provider.Id" />
        <input type="submit" value="Delete" class="btn btn-danger" />
        <a asp-page="./Index" class="btn btn-primary">Back to List</a>
    </form>

</div>
